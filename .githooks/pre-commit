#!/usr/bin/env bash
set -euo pipefail

npm run fmt:check
npm run lint
npm run typecheck
npm test

if command -v gitleaks >/dev/null 2>&1; then
  gitleaks protect --staged
else
  echo "gitleaks is not installed; skipping staged secret scan." >&2
fi
